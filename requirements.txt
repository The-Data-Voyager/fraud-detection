# Core
pandas>=2.0
numpy>=1.26
pyarrow>=14
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44
tabulate>=0.9

# Storage
SQLAlchemy>=2.0
# psycopg2-binary>=2.9      # uncomment for PostgreSQL backend

# Dashboard
streamlit>=1.30
plotly>=5.18

# Streaming (real Kafka backend; the in-process simulator needs none)
# kafka-python>=2.0

# PySpark (Phase 8) — 3.5.x runs on Java 8; Spark 4.x needs Java 17+
pyspark==3.5.3

# GenAI reason layer (Phase 10, optional) — free Groq API
groq>=0.11
